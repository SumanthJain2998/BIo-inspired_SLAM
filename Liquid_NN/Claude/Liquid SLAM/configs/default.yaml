DEFAULT_CONFIG = """
# configs/default.yaml
model:
  type: "cfc"  # Options: cfc, ltc, ncp
  hidden_size: 128
  num_layers: 2
  dropout: 0.1
  
cfc:
  mode: "default"  # Options: default, pure, no_gate
  backbone_activation: "lecun_tanh"  # Options: lecun_tanh, tanh, relu, gelu
  backbone_units: 128
  backbone_layers: 1
  backbone_dropout: 0.0
  
training:
  batch_size: 32
  learning_rate: 0.001
  epochs: 100
  optimizer: "adam"
  scheduler: "cosine"
  warmup_epochs: 5
  gradient_clip: 1.0
  
data:
  dataset: "event_camera"
  input_size: [128, 128]
  sequence_length: 50
  num_workers: 4
  pin_memory: true
  
logging:
  log_dir: "./logs"
  checkpoint_dir: "./checkpoints"
  log_interval: 10
  save_interval: 1000
  use_tensorboard: true
  use_wandb: false
  
evaluation:
  metrics: ["mse", "mae", "tracking_error"]
  benchmark_baseline: true
"""